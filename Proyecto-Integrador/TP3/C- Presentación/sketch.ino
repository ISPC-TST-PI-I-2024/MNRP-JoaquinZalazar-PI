//
// Definir el pin del LED (reemplazar con el número de pin real)
//const int ledPin = 18;

// Configuración inicial
//void setup() {
  // Configurar el pin del LED como salida
  //pinMode(ledPin, OUTPUT);
//}

// Bucle principal
//void loop() {
  // Encender el LED
  //digitalWrite(ledPin, HIGH);
//}


//EJERCICIO 2
// Definir el pin del LED (reemplazar con el número de pin real)
//const int ledPin = 17;

// Intervalo de parpadeo en milisegundos
//const int blinkInterval = 1000;

// Configuración inicial
//void setup() {
  // Configurar el pin del LED como salida
//  pinMode(ledPin, OUTPUT);
//}

// Bucle principal
//void loop() {
  // Encender el LED
  //digitalWrite(ledPin, HIGH);

  // Esperar la mitad del intervalo de parpadeo
 // delay(blinkInterval / 2);

  // Apagar el LED
  //digitalWrite(ledPin, LOW);

  // Esperar la mitad del intervalo de parpadeo
  //delay(blinkInterval / 2);
//}

//EJERCICIO 3
// Arreglo de pines de LED (reemplazar con los números de pin reales)
//const int ledPins[] = {18, 5, 17};

// Longitud de la secuencia
//const int sequenceLength = sizeof(ledPins) / sizeof(ledPins[0]);

// Tiempo de retardo en milisegundos
//const int delayTime = 500;

// Configuración inicial
//void setup() {
  // Configurar los pines de LED como salidas
//  for (int i = 0; i < sequenceLength; i++) {
//    pinMode(ledPins[i], OUTPUT);
//  }
//}

// Bucle principal
//void loop() {
  // Recorrer la secuencia de LED
 // for (int i = 0; i < sequenceLength; i++) {
    // Encender el LED actual
  //  digitalWrite(ledPins[i], HIGH);

    // Esperar el tiempo de retardo
   // delay(delayTime);

    // Apagar el LED actual
   // digitalWrite(ledPins[i], LOW);
 // }
//}


//EJERCICO 4
// Definir el pin del LED (reemplazar con el número de pin real)
//const int ledPin = 18;

// Definir el pin del botón (reemplazar con el número de pin real)
//const int buttonPin = 34;

// Configuración inicial
//void setup() {
  // Configurar el pin del LED como salida
 // pinMode(ledPin, OUTPUT);

  // Configurar el pin del botón como entrada
 // pinMode(buttonPin, INPUT);
//}

// Bucle principal
//void loop() {
  // Leer el estado del botón
  //int buttonState = digitalRead(buttonPin);

  // Si el botón está presionado
  //if (buttonState == HIGH) {
    // Encender el LED
  //  digitalWrite(ledPin, HIGH);
  //} else {
    // Apagar el LED
  //  digitalWrite(ledPin, LOW);
  //}
//}
